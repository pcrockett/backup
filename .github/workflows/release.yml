name: Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: upload release artifacts
        run: bin/release.sh
        env:
          GH_TOKEN: ${{ github.token }}
